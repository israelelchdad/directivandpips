import { Dir5Directive } from './dir5.directive';

describe('Dir5Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir5Directive();
    expect(directive).toBeTruthy();
  });
});
