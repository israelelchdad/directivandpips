import { Dir8Directive } from './dir8.directive';

describe('Dir8Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir8Directive();
    expect(directive).toBeTruthy();
  });
});
