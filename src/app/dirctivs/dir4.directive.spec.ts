import { Dir4Directive } from './dir4.directive';

describe('Dir4Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir4Directive();
    expect(directive).toBeTruthy();
  });
});
