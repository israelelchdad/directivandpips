import { Dir7Directive } from './dir7.directive';

describe('Dir7Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir7Directive();
    expect(directive).toBeTruthy();
  });
});
