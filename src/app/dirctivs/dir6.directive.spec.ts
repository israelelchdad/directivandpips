import { Dir6Directive } from './dir6.directive';

describe('Dir6Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir6Directive();
    expect(directive).toBeTruthy();
  });
});
